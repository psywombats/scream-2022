if getSwitch('got_rat') and getSwitch('got_laptop') and getSwitch('got_diary') and getSwitch('got_report') and getSwitch('got_photo') and getSwitch('got_pendant') then
	speak('ARIEL', "I'm running out of time. I've found a bunch of information already, so maybe I should head to Recurser and record.")
end